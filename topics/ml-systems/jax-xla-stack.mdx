---
title: The JAX/XLA Stack — No Black Boxes
summary: Open the box between your Python and the MXU. How JAX traces a jitted function into StableHLO, how XLA fuses the graph into TPU machine code, JIT vs AOT, and how reading the compiled program lets you catch an OOM before training even starts.
tags: [ml-systems, software, compiler]
difficulty: intermediate
estimatedMinutes: 14
level: 200
prerequisites: []
relatedTo: [ml-systems/tpu-chip-systolic-array, ml-systems/gpu-or-tpu]
videos: []
flashcards:
  - front: What is the JAX → TPU compile path?
    back: A jitted Python function is traced into a static graph → lowered to StableHLO → handed to XLA, which fuses ops and emits an optimized TPU executable. You write NumPy-like Python; XLA writes the machine code.
  - front: What does XLA fusion actually save?
    back: It merges a chain of ops (matmul + bias + activation + scale) into one kernel, so the intermediate results never leave the chip for HBM. Same FLOPs, far fewer HBM round-trips → higher arithmetic intensity.
  - front: JIT vs AOT in JAX
    back: Same XLA compiler, different timing. JIT compiles on the first call (and caches). AOT — jax.jit(f).lower(args).compile() — compiles without running, so you can inspect the HLO and the memory analysis and catch an OOM before you ever touch a slice.
  - front: The JAX AI stack pieces
    back: JAX (numerics + transforms) · Flax NNX (the model/module system — the recommended API; Linen is the older one you'll meet in MaxText) · Optax (optimizers) · XLA/StableHLO underneath. Orbax and Grain handle checkpointing and data.
quiz:
  - question: Why does op fusion raise arithmetic intensity?
    choices:
      - It reduces the number of FLOPs the matmul performs
      - It keeps intermediate results on-chip, so the same FLOPs move far fewer bytes through HBM
      - It increases the clock speed of the MXU
    answer: 1
    explanation: Fusion doesn't change the math — it removes the HBM round-trips between ops. Same FLOPs over fewer bytes means more FLOP/byte, which pushes a memory-bound chain over the ridge toward compute-bound.
  - question: You want to know whether a model will OOM on a v5p slice — without burning the slice. What do you use?
    choices:
      - Just run it and watch
      - AOT lowering — jax.jit(f).lower(args).compile() — then read its memory analysis
      - Add more HBM
    answer: 1
    explanation: AOT compilation produces the executable (and its memory footprint) without devices in the loop, so you can read the compiled memory analysis and catch the OOM at compile time — before a single training step.
  - question: Which is the recommended Flax API for new TPU code today?
    choices:
      - Linen (flax.linen)
      - NNX (flax.nnx)
      - Raw TensorFlow layers
    answer: 1
    explanation: Flax NNX is the recommended API for new code; Linen still exists and powers frameworks like MaxText, so you'll read it — but new models start in NNX. TPUs are XLA/JAX-first, not TensorFlow.
status: published
---

## The one-line summary

L100 ended at *"your code runs on the host and dispatches to the chip."* That's the
black box this level pries open. Between your `model.py` and the
<Term id="mxu">MXU</Term> sits a **compiler** — and once you can read what it did, you
can explain every millisecond and catch an out-of-memory error *before* training
starts. The level's mandate: **no black boxes.**

<Callout type="tip">
**Run it yourself.** This lesson has a companion
[Colab notebook](https://colab.research.google.com/github/thebobrovs/LMS-content/blob/main/notebooks/jax-xla-stack.ipynb)
where you trace a layer into StableHLO, watch XLA's cost model report fewer bytes once it
fuses, reproduce the explorer's `9 → 3` HBM trips, and use AOT lowering to catch an OOM
*before* spending a slice — in real JAX. It runs on the default **CPU** runtime; the closing
benchmark also lights up on a free **`v5e-1`** TPU runtime (`Runtime → Change runtime type`).
</Callout>

## The compile path

You write NumPy-like Python. The TPU runs machine code. Here's the bridge:

1. **Trace.** When you call a `jax.jit`-decorated function the first time, JAX runs it
   once with *abstract* values to record a static graph of operations. (Shapes are
   frozen at trace time — change an input shape and it recompiles. That's the classic
   "why did my step suddenly get slow?")
2. **Lower to StableHLO.** The graph becomes **StableHLO**, a portable, hardware-neutral
   intermediate language.
3. **Compile with <Term id="xla">XLA</Term>.** XLA optimizes the StableHLO — most
   importantly it **<Term id="fusion">fuses</Term>** ops — and emits an executable for
   your target (TPU, GPU, or CPU — the *same* program).

You never hand-write a kernel. You shape the Python; XLA writes the silicon's code.

## Fusion: the optimization that matters most

A transformer layer is a matmul followed by a chain of cheap elementwise ops (bias,
activation, scale). Run them naively and **every op re-reads its input from
<Term id="hbm">HBM</Term> and writes its output back** — a round-trip per op. Fusion
merges the chain into one kernel: the matmul reads its inputs once, the elementwise
work rides along in its *epilogue* on-chip, and only the final result is written back.

Same FLOPs, far fewer bytes — which is exactly the
<Term id="arithmetic intensity">arithmetic intensity</Term> lever from the systolic-array
lesson. Toggle it and watch a memory-bound chain cross the v5p ridge into
compute-bound:

<Simulation id="xla-fusion-explorer" height={520} />

That's the whole game restated in compiler terms: **keep the MXU fed by not wasting
trips to HBM.** Fusion is XLA doing it for you.

## JIT vs AOT — and catching OOM before you spend a slice

Same compiler, two timings:

- **JIT** (`jax.jit`) compiles on the **first call** and caches the result. Convenient,
  but the compile happens with devices in the loop.
- **AOT** (`jax.jit(f).lower(args).compile()`) compiles **ahead of time**, *without
  running*. The payoff: the compiled object carries a **memory analysis** you can read —
  so you can discover that a batch size or sharding choice will **OOM a v5p slice at
  compile time**, before a single step burns real (and scarce) capacity.

On multi-host jobs, a **compilation cache** lets every host reuse one compile instead of
all of them paying the cost — a real startup-time win you'll meet again in the next
lessons.

## Where this sits in the stack

The compiler is the floor. On top of it, the JAX AI stack:

- **JAX** — the numerics and transforms (`jit`, `grad`, `vmap`).
- **Flax NNX** — the model/module system. **NNX is the recommended API** for new code;
  **Linen** is the older one that still powers frameworks like MaxText, so you'll read
  both. (Either way it's **XLA/JAX-first** — *not* "TPUs are for TensorFlow.")
- **Optax** — composable optimizers.
- **Orbax** and **Grain** — checkpointing and data loading (the next lesson's stars).

## Why this matters

Every later skill — SPMD sharding, fixing a starved input pipeline, debugging a
megascale run — assumes you can look at what the compiler produced and reason about it.
The engineer who treats XLA as magic guesses; the one who reads the HLO and the memory
analysis *knows*. That's the difference this level is built to create.

## Put it together

<Steps>
<Step title="Watch fusion move the needle">
In the explorer, flip Unfused → Fused. Read off the HBM round-trips and the arithmetic
intensity, and say in one sentence why the *bound* flipped from memory to compute.
</Step>
<Step title="Place the AOT win">
Explain when you'd reach for `.lower(args).compile()` instead of a plain `jax.jit` call —
and the one thing it lets you check before you spend a slice.
</Step>
<Step title="Name the stack">
List the four layers from your model code down to the silicon (Flax NNX → Optax → JAX →
StableHLO → XLA → TPU) and say which one *fuses* your ops.
</Step>
</Steps>
