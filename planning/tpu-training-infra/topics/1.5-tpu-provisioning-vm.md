# 1.5 Provisioning & The TPU VM Architecture

**Topic id:** `ml-systems/tpu-provisioning-vm` · **Level:** 100 · **Difficulty:** intermediate · **Est:** 12 min
**Status:** planned (awaiting sign-off)

## Why / What / How
- **Why:** In 1.1 the learner understood *what* a TPU VM is. Now they must actually
  **get the hardware and run on it** — and at scale, capacity (not concepts) is the
  wall they hit. Getting provisioning + networking wrong means the job never starts.
- **What:** Provisioning Cloud TPUs operationally — the TPU VM lifecycle, single- vs
  multi-host slices, capacity modes, and the GKE path (node pools, Kueue, DWS Flex).
- **How:** You request a slice (e.g. `v6e-8`, topology `2x4`); GKE + Kueue queue and
  place it; you get root on the host VM(s) attached to the chips; you configure
  networking (MTU/firewall) so a multi-host slice can rendezvous, then run.

## Learning objectives
After this topic the learner can:
- Provision a **single-host** and a **multi-host** TPU slice on GKE.
- Choose a **capacity mode** (on-demand · reservation · Spot · **DWS Flex-start**) for a given need.
- Configure the **networking** a multi-host job needs (MTU 8896, firewall for ICI).
- SSH into the host, run a workload, and inspect the host↔chip mapping.

## Must-learn (keep nothing else but this)
1. You never get a bare chip — you get **host VM(s)** attached to the chips and run on the host.
2. At scale, **capacity is the constraint**; **DWS Flex-start** gives ephemeral, low-cost, *queued* allocation (vs on-demand/reservation/Spot trade-offs).
3. A **multi-host** slice won't run without correct networking (MTU/firewall) — foreshadows the 2.2 rendezvous failure.

## Key terms (glossary additions)
- `tpu vm` — *(exists)* the host VM attached to the chips.
- `dws flex-start` — Dynamic Workload Scheduler Flex-start: ephemeral, queued, low-cost capacity for bursty jobs.
- `kueue` — Kubernetes-native job queueing on GKE (quota, priority, gang-scheduling for multi-host).
- `single-host vs multi-host slice` — one host VM vs several, depending on slice size.
- `capacity mode` — on-demand · reservation · Spot · DWS Flex-start.

## Builds on / sets up
- **Builds on:** 1.1 (TPU VM concept), 1.4 (slice shapes/topology).
- **Sets up:** 2.1 (run JAX on the provisioned host), 2.2 (multi-host rendezvous — the networking you set here).

## Visualization plan
Per the map: **figures + codelab — no rich sim.** Two figures:
- **Fig A — provisioning & execution flow:** workload → Kueue queue → GKE TPU node pool → host VM(s) ↔ chips. (annotated SVG)
- **Fig B — capacity-mode decision:** on-demand / reservation / Spot / DWS Flex-start across *availability · cost · preemption · queueing*. (annotated SVG or a simple comparison block)
- **Lab (codelab):** provision a single-host `v6e` VM on GKE, SSH in, run a JAX matmul; *(stretch)* a 2-host slice with MTU 8896 + firewall.

> **Open call:** figures-only, or add **one small micro-sim** — a *capacity-mode &
> slice-shape selector* (pick a slice + need → see host/chip count + the recommended
> capacity mode and its trade-off)? It's on-standard and cheap, but the map said
> figure-only. Your call.

## Assessment
- **Flashcards:** TPU VM lifecycle; DWS Flex-start; single- vs multi-host; the MTU/firewall requirement; Kueue's role.
- **Quiz:** pick the capacity mode for a bursty experiment vs a guaranteed long run; *why* a multi-host job needs MTU/firewall; what you actually SSH into.

## Sources / notes
- Cloud TPU on GKE docs; Dynamic Workload Scheduler (Flex-start) docs; Kueue docs;
  XPK. Keep figures/labs grounded in current GKE TPU provisioning. (Audit will verify
  capacity-mode names + MTU value.)
