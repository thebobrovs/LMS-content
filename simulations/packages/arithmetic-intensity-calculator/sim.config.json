{
  "id": "arithmetic-intensity-calculator",
  "title": "Arithmetic intensity & the padding trap",
  "version": "1.0.0",
  "props": {},
  "checkpoints": [
    { "id": "observe-roofline", "hint": "Raise the batch size until the workload crosses the ridge into compute-bound — then shrink a dimension off the matrix-unit tile and watch padding waste spike." }
  ]
}
