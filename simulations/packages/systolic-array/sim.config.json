{
  "id": "systolic-array",
  "title": "Systolic array matmul",
  "version": "1.3.0",
  "props": {},
  "checkpoints": [
    {
      "id": "observe-matmul",
      "hint": "Open 'How: the array' and pump through every cycle to watch the array accumulate the matmul on a moving diagonal wavefront, reusing each input."
    }
  ]
}
