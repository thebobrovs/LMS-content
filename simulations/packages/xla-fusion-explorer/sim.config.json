{
  "id": "xla-fusion-explorer",
  "title": "XLA fusion explorer",
  "version": "4.0.1",
  "props": {},
  "checkpoints": [
    { "id": "observe-fusion", "hint": "Walk the compile pipeline (step or auto) to inspect the real jaxpr, StableHLO and fused HLO, then Execute — the fused column's HBM round-trips collapse from 9 to 3, pushing arithmetic intensity past the v5p ridge and flipping the layer from memory-bound to compute-bound." }
  ]
}
